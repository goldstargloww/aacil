{
  "name": "aacil",
  "version": "0.0.0",
  "description": "AAC Image Library website",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "NODE_OPTIONS=--experimental-sqlite webpack --mode=production --config-node-env=production",
    "build:dev": "NODE_OPTIONS=--experimental-sqlite webpack --mode=development",
    "serve": "NODE_OPTIONS=--experimental-sqlite webpack serve"
  },
  "author": "",
  "license": "CC-BY-NC-4.0",
  "dependencies": {
    "csv": "^6.5.1"
  },
  "devDependencies": {
    "webpack": "^5.107.2",
    "webpack-cli": "^7.0.3",
    "webpack-dev-server": "^5.2.4"
  }
}
